{
  "name": "minimal-react-ui-components",
  "version": "0.1.0",
  "private": false,
  "description": "Small React component set (Button, Card) used in my portfolio projects.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "react": "18.2.0"
  },
  "devDependencies": {
    "typescript": "5.3.3"
  }
}
